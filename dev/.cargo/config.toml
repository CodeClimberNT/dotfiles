[target.x86_64-unknown-linux-gnu]
    rustflags = ["-C", "target-cpu=native"]

[build]
    # If sccache is installed on both, keep it global. 
    # If it's Linux-only, wrap it in a conditional.
    rustc-wrapper = "sccache"