# Command to run to update this file

```sh
sed -i "/^\`\`\`txt$/{n;s/.*/$(cargo install --list | grep -E '^[a-zA-Z0-9_-]+ v' | awk '{print $1}' | tr '\n' ' ')/}" dev/.cargo/.cargo-packages.md
```

## Packages

```txt
cargo-update cross tex-fmt 
```